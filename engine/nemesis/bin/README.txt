This chessbot uses its own evaluation technique, If it is wanted, it can be changed with custom evaluators such as stockfish.
Drop the evaluator.exe file to this folder and take out the evaluator(custom).py and change it with base evaluator.py

It will work automatically.